services:
  - type: web
    name: living-universe
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn living_universe.web.server:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
